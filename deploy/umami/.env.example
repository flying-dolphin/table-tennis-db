# 复制为 .env 并填入真实值，不要提交到 git
# 生成命令（在服务器上运行一次）：
#   UMAMI_DB_PASSWORD=$(openssl rand -hex 24)
#   UMAMI_APP_SECRET=$(openssl rand -hex 32)
#   echo "UMAMI_DB_PASSWORD=$UMAMI_DB_PASSWORD"
#   echo "UMAMI_APP_SECRET=$UMAMI_APP_SECRET"

UMAMI_DB_PASSWORD=
UMAMI_APP_SECRET=
